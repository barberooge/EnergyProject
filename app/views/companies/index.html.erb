<% provide(:title, 'All companies') %>
<h1>All companies</h1>

<ul class="companies">
  <% @companies.each do |company| %>
    <li>
      <%= link_to company.name, company %>
    </li>
  <% end %>
</ul>
